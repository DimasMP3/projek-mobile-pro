import 'package:flutter/material.dart';

// ============================================
// LUXURY CINEMA COLOR PALETTE
// Deep dark with gold/amber accents
// ============================================

// Primary Colors
const Color primary = Color(0xFFC5A059);  // Champagne Gold
const Color primaryLight = Color(0xFFD4B483);
const Color primaryDark = Color(0xFF8B6B3F);

// Background Colors
const Color bg = Color(0xFF030305);  // Deep Onyx
const Color bgSecondary = Color(0xFF0C0C12);  // Night Sky
const Color surface = Color(0xFF12121A);  // Card/Surface Dark
const Color surfaceLight = Color(0xFF1A1A26);  // Elevated Surface

// Text Colors
const Color textPrimary = Color(0xFFF5F5F7); // Off-white
const Color textSecondary = Color(0xFFA1A1A6);
const Color textTertiary = Color(0xFF6E6E73);
const Color textGold = Color(0xFFC5A059);

// Accent Colors
const Color accentGold = Color(0xFFC5A059);
const Color accentChampagne = Color(0xFFE8D5B7);
const Color accentBronze = Color(0xFFCD7F32);
const Color accentGreen = Color(0xFF32D74B);
const Color accentRed = Color(0xFFFF453A);

// Gradient Colors
const Color gradientStart = Color(0xFF12121A);
const Color gradientEnd = Color(0xFF030305);

// Glass Effect Colors
Color glassWhite = Colors.white.withValues(alpha: 0.05);
Color glassBorder = Colors.white.withValues(alpha: 0.1);
Color glassGold = const Color(0xFFC5A059).withValues(alpha: 0.12);

// Glow Effects
Color glowGold = const Color(0xFFC5A059).withValues(alpha: 0.35);
Color glowWhite = Colors.white.withValues(alpha: 0.15);
