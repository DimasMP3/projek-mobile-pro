import 'package:flutter/material.dart';

// ============================================
// LUXURY CINEMA COLOR PALETTE
// Deep dark with gold/amber accents
// ============================================

// Primary Colors
const Color primary = Color(0xFFD4AF37);  // Cinema Gold
const Color primaryLight = Color(0xFFE8C85C);
const Color primaryDark = Color(0xFFB8962E);

// Background Colors
const Color bg = Color(0xFF0A0A0F);  // Deep Black
const Color bgSecondary = Color(0xFF12121A);  // Dark Slate
const Color surface = Color(0xFF1A1A24);  // Card/Surface Dark
const Color surfaceLight = Color(0xFF242432);  // Elevated Surface

// Text Colors
const Color textPrimary = Colors.white;
const Color textSecondary = Color(0xFFB3B3B3);
const Color textTertiary = Color(0xFF7A7A7A);
const Color textGold = Color(0xFFD4AF37);

// Accent Colors
const Color accentGold = Color(0xFFD4AF37);
const Color accentChampagne = Color(0xFFE8D5B7);
const Color accentGreen = Color(0xFF22C55E);
const Color accentRed = Color(0xFFEF4444);

// Gradient Colors
const Color gradientStart = Color(0xFF1A1A24);
const Color gradientEnd = Color(0xFF0A0A0F);

// Glass Effect Colors
Color glassWhite = Colors.white.withValues(alpha: 0.08);
Color glassBorder = Colors.white.withValues(alpha: 0.15);
Color glassGold = const Color(0xFFD4AF37).withValues(alpha: 0.15);

// Glow Effects
Color glowGold = const Color(0xFFD4AF37).withValues(alpha: 0.4);
Color glowWhite = Colors.white.withValues(alpha: 0.2);
